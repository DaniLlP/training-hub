# assets/

Reserved for static imagery (illustrations, exercise stills, backgrounds).

The app currently ships **icon-only** and draws all visuals (rings, radar, charts) on
`<canvas>`, so no bitmap assets are required to run. Drop optimized images here (WebP/SVG)
and reference them with relative paths from `script.js` or `styles.css` if you add any.
